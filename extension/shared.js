export async function api(path,body){const c=await chrome.storage.local.get(['groveUrl','groveToken','groveWorkspace']);if(!c.groveUrl||!c.groveToken)throw new Error('Open Settings to connect your Grove workspace.');const response=await fetch(c.groveUrl+'/api'+path,{method:body?'POST':'GET',headers:{Authorization:'Bearer '+c.groveToken,'X-Workspace-Id':c.groveWorkspace,...(body?{'Content-Type':'application/json'}:{})},body:body?JSON.stringify(body):undefined});const data=await response.json();if(!response.ok)throw new Error(data.error||'Grove request failed');return data;}
export async function captureTab(tabId,selection){const [{result}]=await chrome.scripting.executeScript({target:{tabId},func:()=>({url:location.href,title:document.title,selection:window.getSelection()?.toString()||'',content:(document.querySelector('article')||document.querySelector('main'))?.innerText?.slice(0,45000)||'',description:document.querySelector('meta[name="description"]')?.content||''})});if(!/^https?:/.test(result.url))throw new Error('Capture a regular web page.');return {...result,selection:selection||result.selection};}
